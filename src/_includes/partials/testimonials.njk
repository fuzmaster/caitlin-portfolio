<section class="section testimonials" aria-labelledby="testimonials-heading">
  <div class="l-shell">
    <p class="kicker">References</p>
    <h2 id="testimonials-heading">Professional references available</h2>
    <ul class="testimonials__list">
      {% for item in testimonials %}
      <li class="testimonial-card">
        <p>{{ item.quote }}</p>
        <p class="testimonial-card__meta">{{ item.name }} - {{ item.company }}</p>
      </li>
      {% endfor %}
    </ul>
    <p class="references-note">References available upon request.</p>
  </div>
</section>
